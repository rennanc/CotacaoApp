@model IEnumerable<CotacaoApp.Models.Seguradora>

@{
    ViewBag.Title = "Seguradora";
}

<h2>Seguradora</h2>

<p>
    @Html.ActionLink("Nova Seguradora", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SinistroAuto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Recepcao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Outros)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calculos)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SinistroAuto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recepcao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Outros)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Calculos)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }) |
                @Html.ActionLink("Deletar", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
